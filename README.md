# django_blog

```shell
pip3 install djangorestframework
pip3 install django-cors-headers
pip3 install django-cleanup
pip3 install pillow
pip3 install markdown

# in /django_blog
python manage.py runserver
```

```shell
yarn add @vue/cli
yarn add vue-analytics

# in /django_blog/frontend
yarn run serve
```